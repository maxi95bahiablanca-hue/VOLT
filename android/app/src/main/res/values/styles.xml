<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Android 15 (API 35) dejó obsoletos statusBarColor y enforceNavigationBarContrast:
       con edge-to-edge (app.json → android.edgeToEdge) las barras son transparentes y
       esos atributos ya no hacen nada, pero Play los marca como "APIs obsoletas para la
       vista de extremo a extremo". El color de los iconos lo maneja <StatusBar style="light" />
       (expo-status-bar) en App.js, así que sacarlos no cambia cómo se ve la app.
       ⚠️ Si alguna vez se corre `npx expo prebuild --clean`, este archivo se regenera:
       revisar que no vuelvan a aparecer. -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
</resources>